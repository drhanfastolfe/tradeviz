{
  "name": "tradeviz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "tsc --noEmit",
    "test:e2e": "playwright test",
    "test:e2e:deployed": "PLAYWRIGHT_BASE_URL=https://drhanfastolfe.github.io/tradeviz/ playwright test"
  },
  "dependencies": {
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
